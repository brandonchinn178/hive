_common/lib: !include "../hpack.yaml"
<<: *defaults

name: hive-client

dependencies:
  - base
  - containers
  - hive
  - react-flux

library:
  source-dirs:  src

executable:
  source-dirs:  exe
  main:         Main.hs
  dependencies:
    - hive-client
